<h3><%= @user.name %>さんのコメント一覧</h3>

<table>
  <thead>
    <tr>
      <th>レシピ名</th>
      <th>ユーザー名</th>
      <th>コメント</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @comment_recipes.each do |recipe| %>
    <tr>
      <td><%= recipe.name %></td>
      <td><%= recipe.user.name %></td>
      <td><%= recipe.comment %></td>
      <td><%= link_to "削除", admin_comment_path(comment.id), method: :delete, class:"btn btn-danger" %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @comments %>